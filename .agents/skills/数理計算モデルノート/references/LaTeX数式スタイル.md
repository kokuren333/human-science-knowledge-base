# LaTeX 数式スタイル

- インライン数式は `$...$` を使う。
- ブロック数式は `$$...$$` を使う。
- 記号を初出で定義する。
- 長すぎる式は分割し、式の前後に意味を書く。
- 添字や上付きは読みやすさを優先する。

## 例

```markdown
予測誤差 $\delta_t$ は、得られた報酬 $r_t$ と予測価値 $V(s_t)$ のずれとして書ける。

$$
\delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)
$$

ここで $\gamma$ は将来報酬の割引率である。
```
